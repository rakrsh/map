[project]
name = "geocoding-service"
version = "0.1.0"
description = "Geocoding and address search service"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110.0",
  "uvicorn>=0.29.0",
  "elasticsearch>=8.0.0",
  "pydantic>=2.6.0"
]
